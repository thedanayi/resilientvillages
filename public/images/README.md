You can add your custom images to this folder. 

When you upload an image here (e.g., `my-image.jpg`), you can reference it directly in your code using a starting slash:

```tsx
<img src="/images/my-image.jpg" alt="Description" />
```
